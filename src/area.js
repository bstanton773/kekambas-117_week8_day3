// Pure JS here - no type checking

// below is not a comment (once we turn on checkJS)
// this tell TS not to type check this file

// @ts-nocheck

export function squareFootage(length, width){
    return length * width
}
