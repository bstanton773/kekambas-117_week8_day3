// Pure JS here - no type checking

// JSDocStrings start /** and end */
// They can assign parameters and return types to function

/**
 * @param {number} length
 * @param {number} width
 * @returns {number}
 */
export function squareFootageJD(length, width){
    return length * width
}
